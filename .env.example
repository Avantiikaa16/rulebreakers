# Optional. Without it, hypothesis classification and the tutor report use the
# deterministic fallback and the game is fully playable.
ANTHROPIC_API_KEY=

# Optional override (defaults to claude-sonnet-5)
# RULEBREAKERS_MODEL=claude-sonnet-5
