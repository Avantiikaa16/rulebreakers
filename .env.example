# Optional. Without either key, hypothesis classification and the tutor report
# use the deterministic fallback and the game is fully playable.
#
# Groq is checked first (free tier, no billing needed) — console.groq.com
GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile

# Used only if GROQ_API_KEY is not set — platform.openai.com
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
